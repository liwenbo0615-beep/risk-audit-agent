# DeepSeek API
DEEPSEEK_API_KEY=sk-ba12d208acef4d45803866b9d093520d
DEEPSEEK_MODEL=deepseek-chat
DEEPSEEK_BASE_URL=https://api.deepseek.com

# 审核日志路径（绝对或相对路径均可）
AUDIT_LOG_PATH=audit_log.jsonl

# 离线演示模式（1=开启，0=使用真实 LLM）
OFFLINE_DEMO_MODE=1

# LLM 超时（秒）和重试次数
LLM_TIMEOUT=8
LLM_MAX_RETRIES=0

# 批处理 / API 下的默认人工决策（approve/reject/skip，留空则触发交互式输入）
AUTO_REVIEW_DECISION=
